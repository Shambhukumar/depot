<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div align="right">
  <div class="field" >
   
    <%= f.text_field :title, :id => 'field', :placeholder => "Title" %>
  </div>
  <div class="are">
    
    <%= f.text_area :description, :rows => 6, :id => 'field', :placeholder => "Description" %>
  </div>
  <div class="field">
  
    <%= f.text_field :image_url, :id => 'field', :placeholder => "Image URL" %>
  </div>
  <div class="field">
   
    <%= f.text_field :price, :id => 'field', :placeholder => "Price" %>
  </div>
  <div class="actions">
    <%= f.submit 'Create Product', :id => 'button' %>
  </div>
  </div>
  
<% end %>
